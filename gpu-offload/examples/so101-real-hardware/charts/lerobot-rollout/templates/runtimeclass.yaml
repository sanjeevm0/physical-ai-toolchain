{{- if and .Values.runtimeClass.create .Values.runtimeClass.name }}
apiVersion: node.k8s.io/v1
kind: RuntimeClass
metadata:
  name: {{ .Values.runtimeClass.name }}
  labels:
    {{- include "lerobot-rollout.labels" . | nindent 4 }}
handler: {{ .Values.runtimeClass.handler }}
{{- end }}
